# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
 #images_directory: "%kernel.project_dir%/public/uploads/images"
 images_directory: "%kernel.project_dir%/public/uploads/images"

 uploads_directory: '%kernel.project_dir%/public/uploads'
 restaurant_images_directory: 'C:/Users/MSI/testing1/testing/public/uploads/restaurants'
 openai_api_key: "%env(OPENAI_API_KEY)%"
 github_token: "%env(GITHUB_TOKEN)%"
 
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
     # Configuration de la session
    session.storage.native:
        class: Symfony\Component\HttpFoundation\Session\Storage\NativeSessionStorage
        arguments:
            - []
            - '@session.handler.native'
            - '@session.storage_metadata'

    session.handler.native:
        class: Symfony\Component\HttpFoundation\Session\Storage\Handler\NativeFileSessionHandler

    session.storage_metadata:
        class: Symfony\Component\HttpFoundation\Session\Storage\MetadataBag

    session:
        class: Symfony\Component\HttpFoundation\Session\Session
        arguments:
            - '@session.storage.native'
            - '@session.storage_metadata'

    # Service Panier
    App\Service\PanierService:
        arguments:
            $requestStack: '@request_stack'  # Utilisez RequestStack pour obtenir la session
    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    # config/packages/security.yaml

  
